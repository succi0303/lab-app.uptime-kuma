version: '3.9'
services:
    uptime-kuma:
        image: louislam/uptime-kuma:1
        container_name: uptime-kuma
        volumes:
            - data:/app/data
        ports:
            - '3007:3001'
        restart: always
        network_mode: host
volumes:
  data:
